# 交给开发 Agent 的提示词

> 用法：把下面分隔线之间的内容整份发给开发 Agent。
> **每次只做一个阶段**——改第一行的 `R0` 为 `R1`/`R2`/`R3`/`R4` 即可复用。
> R2 那一轮记得把「前端 §7 同期改」这句留着（其余阶段它不适用，但留着无害）。

---

你在 `forge-solt` 仓库里实施「审核返修」功能的 **R0** 阶段。

## 第一步：先读，读完先回话，不要动手

按顺序读完这四份，然后**停下来**：

1. `notes/REVIEW-IMPLEMENTATION-PLAN-R0-R4.md` —— 实施文档，你的主要依据
2. `notes/AUTOMATED-REVIEW-REVISION-DESIGN-V0.2.md` —— 设计决策 D-21…D-32、验收 AC-R-001…017
3. `notes/R0.5-ADDENDUM-PROMPT-ARCHITECTURE.md` —— 这些决策背后的实测数据
4. 实施文档里本阶段点名的每个源文件

读完先回答我三件事，**得到我确认后再写第一行代码**：

- 本阶段你要改哪些文件、各自改什么（列清单）；
- 实施文档 §8 的七个坑里，哪几个落在本阶段；
- 你发现的任何「文档说的和代码实际不一样」的地方。

第三点尤其重要：**文档已经过一轮人工核对，但仍可能有错。**
发现不一致时**报告，不要自行决定**采信哪边。

## 工作纪律（违反其中任何一条，这次改动就是坏的）

**1. 每条断言必须先看着它失败。**
写完一个 `expect`，去把产品代码改坏，确认它变红，再改回来。
没红过的断言是装饰品。实施文档里标了「**必须反证**」的地方，
是最容易写成装饰的地方，交付时要逐条报告你看到它红了。

**2. 不要顺手重构。**
仓库里有已知的三处枚举重复（Zod / SQL CHECK / domain 表）。
**本次不消除它。** 重构混进来会让这次的 diff 没法审。

**3. 不要改需求文档。**
REQ、术语表、技术方案、UI Spec 都已经改到位了。
实现跟文档冲突时停下来报告，不要改文档迁就实现。

**4. 不要扩大范围。**
只做本阶段。看到下个阶段的东西「顺手也能做」——不要做。
看到不相关的 bug——记下来告诉我，不要修。

**5. 分层是 ESLint 强制的。**
`shared ← domain（纯函数，零 IO）← application ← runtime/infrastructure ← api`。
`src/server/domain/**` 强制 100% 分支/函数/行/语句覆盖。
往 domain 里放任何 IO 都会被 lint 拦下。

## 完成的定义

实施文档 §9 写了每个阶段的完成判据。除此之外，这四条命令必须真的跑过且全绿：

```bash
npm run typecheck
npm run lint
npm run test
npm run test:coverage
```

**R0 阶段特别注意：不该让任何现有断言变红。**
红了说明你的改动语义超出了预期——停下来查，不要改测试去迁就。

## 交付时报告这些

1. 四条命令的**实际输出**（粘贴，不要只说「通过了」）；
2. 每条「必须反证」的断言：你把什么改坏了、看到哪条红了；
3. 你偏离实施文档的地方，以及为什么；
4. 你发现但没修的问题。

## 不要做的事

- 不要 commit（我来）；
- 不要碰 `data/*.sqlite`——那几个库**没有备份**，`m4-*` 和 `m7-*` 是所有实测数据的唯一副本，只读；
- 不要碰 `probe/` 下的既有脚本和结果——它们是 R4 回归的基线；
- 不要在任何文件里写入 API Key 或密钥值（`.env` 存值且已被 git 忽略；`config/providers.yaml` 只放环境变量**名**）。

## 关于这个功能，有一件事你必须理解对

审核 Agent 只有**两条判据实测有效**，另两条在三种提问架构下都是 0/3。
四条**全部上线**——测不出来的照样实现，因为它们误报为 0（不添乱），
且删掉就再也观察不到它们何时开始生效。

**不要因为判据三/四测不出来就跳过它们，也不要试图去「优化」它们。**

由此还有一条**正确性要求**（不是文案偏好）：
系统任何输出——trace kind、API 字段、UI 文案、错误消息、代码注释——
**不得出现「审核通过」「质量合格」「已校验」**。统一用「未检出问题」。

理由：这个探测器只会漏、几乎不会冤，说「通过」是不实陈述。
项目有一条规矩是「错误消息不能撒谎」，这里是它的延伸。

---

## 后续阶段的改动点（给你自己留着）

| 阶段 | 改第一行为 | 额外交代 |
|---|---|---|
| R1 | `R1` | 三个 domain 纯函数，覆盖率必须仍是 100% |
| R2 | `R2` | **前端与后端同期改**，见实施文档 §7；不改前端会让界面看起来卡死 |
| R3 | `R3` | AC-R-014 的反证要求最容易被糊弄，重点看 |
| R4 | `R4` | 完成判据是拿 `probe/cases.json` 跑回归，门槛项与基线项**不许合并成一个数** |

---

# 第二轮：R0 开工确认（回复它的七点之后发这份）

> 用法：下面分隔线之间整份发给开发 Agent。

---

你提的七点我逐条核过了。**三条属实，其中 PRAGMA 那条是我文档的硬错误。**
文档已按你的意见更新并提交。

**动手前先重读 `notes/REVIEW-IMPLEMENTATION-PLAN-R0-R4.md` 的 §2**——
改动集中在那里，特别是新增的 §2.3.1。

## 你的发现：批准

**PRAGMA foreign_keys=OFF 用不了** —— 你是对的。我核了 `migrate.ts:95`，
确实是 `db.transaction(() => db.exec(sql))`，该 PRAGMA 在事务内是 no-op。
**按你的方案走**：事务内重建 + 测试兜底，不改 `migrate.ts`。

你主动提到的 rename 方向陷阱也对，我把它写成死规矩了：
`建新表(别名) → 拷数据 → DROP 原表 → RENAME 新表为原名`，
**绝不能先把原表 rename 走**。

## 但有一处你把风险方向说反了

你把「现有测试不会变红」当作放心的理由。
**在 `src/server/domain/state-machine.test.ts` 这里，绿正是危险本身。**

第 16 行的 `SLOT_STATUSES` 和第 107 行循环里的 action 数组**都是硬编码的本地清单**。
加了 `reviewing` 和三个新动作之后：

- 测试**依然全绿**（`SlotStatus[]` 的子集仍然类型合法）；
- 但覆盖从 4×5=20 格，变成 5×8=40 格里的 **20 格**——**一半**。

**一个自称穷尽的测试，会悄悄不再穷尽。**

而且 **100% 分支覆盖救不了**：`canSlotTransition` 就是一次表查找，
整个函数一个分支，覆盖率照样满分，20 个表格从没被碰过。
**覆盖率不保护表驱动的数据。**

**必须改**：两层循环从已导出的 `SLOT_ACTIONS`（`state-machine.ts:138`）推导，
状态清单同样导出一份并用它；**外加**为五条新迁移各写一条显式断言。
这不属于 §0.3 禁止的顺手重构——理由见 §2.3.1。

## 你问的四个决策点

| # | 决定 | 理由 |
|---|---|---|
| 1 domain 两字段 | **R0 就加** | 库有列而类型没有，会让 R1/R2 到处绕，中间留一段库与 domain 不一致的窗口。`types.ts` 已被 `vitest.config.ts:21` 排除在覆盖率外，不影响门槛 |
| 2 `revision_round` 递增 | **合进 `markForRevision` 的同一条 SQL** | 见下 |
| 3 接 `buildRepositories` | **接** | 否则 R2 还要再动一次装配 |
| 4 三个标签 | **照你拟的用** | 提交审核 / 审核结算 / 返修，都避开了「通过/合格」 |

第 2 条要强调：**这不是整洁性偏好，是防死循环。**
拆成两步会留下「状态回了 `pending` 但计数没加」的路径——
**那等于返修预算永不推进，循环无限跑下去**，正是 D-26 要防的东西。
合进同一条 UPDATE 则物理上不可能发生。
因此 §4.4 的结算事务里**没有**单独的递增步骤，加了就是双倍计数。

## 你另外三处的判断：确认

- **两份文档阶段划分不一致** —— 你说对了。**以实施文档为准**，V0.2 §9 已加注说明它是粗粒度草图。
- **Operation 的阶段归属** —— 你的理解正确，已写进文档：R0 只加枚举值，引擎接线归 R2，`skill-loader` 的 `superRefine` 归 R4 §6.2。
- **附录 §6 与 D-28 的矛盾** —— **这条捕捉得好。** 附录那条「首版只上两条判据」确实已被 D-28 推翻。我在附录 §6 顶部加了推翻标注，防止后续阶段有人读了它去裁判据。

## 我顺带简化的两处（文档已改，按新版做）

- **删掉 `ALTER TABLE ADD COLUMN` 两步** —— `slots` 反正要重建，直接把两列写进新表定义，拷数据时 `INSERT INTO slots_new SELECT ..., 0, 0 FROM slots`。原文那两步是多余的。
- **`slot_reviews` 改到最后建** —— 它对两张被重建的表都有外键，最后建可以整类绕开建表顺序的疑问。

## 你报的额外发现

`countByStatus` 零调用方——属实，我全库搜过。**但这次不要碰**，属于顺手重构。
记着它，这轮结束后单独清理。

## 开始 R0

按更新后的文档执行。交付时按第一轮说的报告：

1. 四条命令的**实际输出**（粘贴，不要只说「通过了」）；
2. 每条「必须反证」的断言：你把什么改坏了、看到哪条红了；
3. 偏离文档的地方及原因；
4. 发现但没修的问题。

R0 特别注意：**除了 `state-machine.test.ts` 那两份清单是有意扩充的，
其余现有断言一条都不该变红。** 红了说明改动语义超出预期——
停下来报告，不要改测试去迁就实现。
