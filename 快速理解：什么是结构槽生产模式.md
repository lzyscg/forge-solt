# 快速理解：什么是结构槽生产模式

## 1. 从第一性原则出发

内容生产最基本的目标是：

```text
输入 → 最终产物
```

无论生产的是文章、小说章节、歌词、报告、剧本还是其他内容，本质上都是将用户提供的目标、资料和约束，转化为一个可以交付的最终产物。

结构槽生产模式在输入与最终产物之间，增加了一层结构化的内容表示：

```text
用户输入
  ↓
创建具体结构
  ↓
形成若干内容槽位
  ↓
逐槽生产内容
  ↓
确定性组装
  ↓
最终产物
```

可以形式化为：

```text
Artifact = Assemble(Structure, SlotContents)
```

其中：

- `Structure` 决定最终产物由哪些部分组成，以及这些部分之间的顺序、层级和依赖；
- `SlotContents` 是每个内容槽位中实际生产出的内容；
- `Assemble` 决定系统如何将所有槽位内容组合为最终文件；
- `Artifact` 是最终可查看、保存和交付的完整产物。

这构成了结构槽生产模式最基础、不可再删减的生产闭环。

---

## 2. 结构槽生产的本质

结构槽生产不是把一篇完整内容机械地切成若干文本片段，也不是在界面中创建多个普通文本框。

它的本质是：

> **先将一个复杂内容目标拆解为多个具有明确职责的内容工作单元，再分别完成这些工作单元，最后重新组装成完整产物。**

例如，一章小说可以拆解为：

```text
章节
├── 标题
├── 开场
├── 场景一
├── 场景二
├── 场景三
├── 情绪落点
└── 章节结尾
```

这些部分不是按照字数平均划分的，而是分别承担不同的内容职责：

- 标题负责概括或承接本章核心变化；
- 开场负责让读者进入当前场景；
- 场景段负责推进人物行动、冲突和信息变化；
- 情绪落点负责形成阶段性的情绪回声；
- 章节结尾负责留下尚未解决的问题或下一步行动。

每一个这样的内容工作单元，就是一个**结构槽**。

---

## 3. Agent、Skill 与结构槽的关系

结构槽不会自行创建，也不会自行产生内容。

内容层面的工作始终由 Agent 执行。

完整关系是：

```text
Agent + Skill + 工作上下文
            ↓
      对结构槽执行动作
            ↓
   创建结构 / 填充槽位内容
```

其中：

- **Agent 是内容生产主体**，负责做出语义、结构、表达和创作判断；
- **Skill 是 Agent 的工作方法**，规定这项工作应当如何完成；
- **结构槽是 Agent 的工作对象**，规定当前要处理哪个内容单元；
- **工作上下文是 Agent 的生产依据**，包含任务输入、槽位目标和必要的前置内容；
- **系统是生产过程的控制者**，决定什么时候工作、由哪个 Agent 使用哪个 Skill、如何保存结果以及何时组装产物。

可以进一步形式化为：

```text
Structure =
  StructureAgent(
    StructureSkill,
    TaskInput
  )
```

```text
SlotContent[i] =
  ContentAgent(
    SlotSkill[i],
    Slot[i],
    SlotContext[i]
  )
```

```text
Artifact =
  Assemble(
    Structure,
    SlotContents
  )
```

因此，结构槽并没有取代 Agent。

结构槽的作用是将 Agent 的工作对象结构化，Skill 的作用是将 Agent 的工作方法标准化，系统的作用是将整个生产过程确定化。

---

## 4. 结构槽模式的基础生产流程

一次完整的结构槽生产任务包含三个主要内容阶段。

### 第一阶段：创建结构

结构设计 Agent 根据用户输入，使用结构设计 Skill，判断最终产物需要由哪些部分组成。

Agent 负责决定：

- 创建哪些槽位；
- 每个槽位承担什么内容职责；
- 槽位之间如何排列；
- 哪些槽位需要依赖前置内容。

系统负责检查：

- 槽位是否合法；
- ID 是否重复；
- 父子关系是否成立；
- 依赖是否成环；
- 槽位类型是否在模板允许范围内。

结构通过校验后，系统保存本次任务的具体结构。

---

### 第二阶段：填充槽位

系统根据当前结构和槽位状态，选择已经具备生产条件的槽位。

随后，系统根据模板确定：

```text
目标槽位
→ 对应 Agent
→ 对应 Skill
```

并为 Agent 构建明确的工作上下文：

```text
Agent 角色
+ 当前 Skill
+ 任务输入
+ 当前槽位
+ 当前槽位目标
+ 必要的依赖槽位内容
```

Agent 使用指定 Skill 完成当前槽位内容，并将结果提交给系统。

系统校验并保存结果，然后继续推进下一个槽位。

---

### 第三阶段：组装产物

当所有必要的内容槽位都已完成后，系统按照具体结构中声明的顺序和层级，将槽位内容确定性组装为最终文件。

例如：

```text
标题内容
+ 开场内容
+ 场景一内容
+ 场景二内容
+ 场景三内容
+ 情绪落点内容
+ 章节结尾内容
→ chapter.md
```

组装器只负责排序、连接和输出文件，不负责进行新的语义创作。

如果最终阶段还需要补写过渡内容、统一表达或重写正文，这类工作必须被显式定义为一个结构槽，再由 Agent 使用相应 Skill 完成。

---

## 5. 结构槽生产模式的五个基础原则

### 原则一：内容由 Agent 生产

所有需要语义理解、判断和创造的内容，都必须由 Agent 完成。

系统不直接创作内容。

---

### 原则二：Agent 必须使用明确的 Skill

每一次 Agent 工作都必须绑定一个明确的工作方法。

Agent 不根据随机上下文自由选择工作方式，也不在一次执行中加载所有 Skill。

---

### 原则三：Agent 必须有明确的工作对象

每一次 Agent 执行都必须对应：

- 一次具体结构创建任务；或
- 一个具体结构槽。

Agent 不得在没有明确目标的情况下自由修改整个产物。

---

### 原则四：上下文由系统确定性构建

Agent 的连续性不依赖聊天历史，而依赖：

- 冻结的任务输入；
- 当前结构；
- 当前槽位目标；
- 显式声明的前置槽位内容；
- 当前 Skill。

相同任务状态应能够重新构建出相同的工作上下文。

---

### 原则五：最终完成由系统判断

Agent 只提交内容结果。

系统负责：

- 保存结构；
- 保存槽位内容；
- 推进槽位状态；
- 判断所有必要槽位是否完成；
- 组装最终产物；
- 将任务设置为完成。

Agent 不能通过自然语言自行宣布任务已经完成。

---

## 6. 结构槽生产不等于什么

结构槽生产本身不是审核系统。

审核、Finding、返修授权和权威证明都可以在未来作为附加能力建立，但不是结构槽生产成立的前提。

结构槽生产本身也不是多 Agent 系统。

一个最简单的结构槽生产任务可以只使用：

- 一个结构设计 Agent；
- 一个内容生产 Agent。

多 Agent 分工是结构槽生产之上的组织方式，而不是结构槽模式的必要条件。

结构槽生产也不是自由聊天工作流。

Agent 不依赖长期对话自由推进任务，而是在系统创建的明确工作指派中，对明确对象执行明确动作。

---

## 7. 一句话定义

> **结构槽生产是一种由 Agent 使用明确 Skill，将复杂内容目标拆解为若干具有明确职责和依赖的内容槽位，逐槽完成内容生产，并由系统确定性组装为最终产物的生产方式。**

其最小生产闭环为：

```text
Agent 创建结构
→ 系统校验结构
→ Agent 使用 Skill 填充槽位
→ 系统保存并推进状态
→ 系统组装最终产物
```

后续的审核、返修、版本、关系、迁移和并发能力，都应建立在这一基础生产闭环已经稳定运行的前提之上。